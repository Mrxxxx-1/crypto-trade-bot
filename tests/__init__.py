"""Test package.

Present so ``python -m unittest discover -s tests -t .`` can import the suite,
and so ``from tests.helpers import ...`` resolves the same way under discovery
as it does when a module is run directly.
"""
